/**
 * Animation timing constants from the PRD.
 */

/** Duration for entrance animations in ms. */
export const ENTRANCE_DURATION = 400;

/** Delay between staggered entrance elements in ms. */
export const ENTRANCE_STAGGER = 80;

/** Duration for check phase transitions in ms. */
export const CHECK_TRANSITION_DURATION = 300;

/** Delay between staggered result elements in ms. */
export const RESULT_STAGGER = 120;

/** Duration for score count-up animation in ms. */
export const SCORE_COUNT_UP_DURATION = 800;

/** Scale multiplier for correct button press feedback. */
export const BUTTON_CORRECT_SCALE = 1.08;

/** Duration for button shake animation on incorrect input in ms. */
export const BUTTON_SHAKE_DURATION = 300;

/** Duration for trigger completion flash in ms. */
export const TRIGGER_COMPLETION_DURATION = 200;

/** Duration for target hit animation in ms. */
export const TARGET_HIT_DURATION = 300;

/** Initial scale of targets. */
export const TARGET_SCALE_FROM = 1;

/** Target scale on hit. */
export const TARGET_SCALE_TO = 1.3;
