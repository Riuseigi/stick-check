"use client";

export default function CheckPage() {
  return (
    <div className="flex flex-1 items-center justify-center min-h-screen bg-[var(--bg-canvas)] text-[var(--text-primary)]">
      <div className="text-center">
        <h1 className="text-2xl font-semibold tracking-tight">Controller Check</h1>
        <p className="mt-2 text-sm text-[var(--text-secondary)]">Check flow will be implemented in Phase 3+</p>
      </div>
    </div>
  );
}
